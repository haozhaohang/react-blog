.record-wrapper {
    display: flex;
    max-width: 1200px;
    min-width: 900px;
    margin: 20px auto;

    .record-main {
        flex: 1;
        padding-right: 20px;
    }

    .record-aside {
        width: 300px;
    }
}